<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Portfolio – Peniel Mabanza</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
<script>
    // Apply saved theme on load
    document.addEventListener("DOMContentLoaded", () => {
      const theme = localStorage.getItem("theme") || "dark";
      if (theme === "light") {
        document.documentElement.classList.remove("dark");
      }
    });

    // Toggle theme
    function toggleTheme() {
      const html = document.documentElement;
      const isDark = html.classList.contains("dark");

      if (isDark) {
        html.classList.remove("dark");
        localStorage.setItem("theme", "light");
      } else {
        html.classList.add("dark");
        localStorage.setItem("theme", "dark");
      }
    }
  </script>

  <style>
    html { scroll-behavior: smooth; }
     .reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 0.8s cubic-bezier(.17,.67,.54,1.03);
  }

  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }

  .zoom {
    opacity: 0;
    transform: scale(0.95);
    transition: all 0.8s ease-out;
  }

  .zoom.active {
    opacity: 1;
    transform: scale(1);
  }
  </style>
</head>

<body class="bg-gray-900 text-gray-200">

  <!-- NAV -->
  <nav class="fixed top-0 w-full bg-gray-900/80 backdrop-blur-md z-50">
    <div class="max-w-6xl mx-auto flex items-center justify-between p-4">
      <h1 class="text-xl font-bold">Peniel.</h1>

      <div class="hidden md:flex gap-6 text-gray-300">
        <a href="#hero" class="hover:text-white">Accueil</a>
        <a href="#about" class="hover:text-white">À propos</a>
        <a href="#skills" class="hover:text-white">Compétences</a>
        <a href="#projects" class="hover:text-white">Projets</a>
        <a href="#contact" class="hover:text-white">Contact</a>
      </div>
    </div>
  </nav>

  <!-- HERO -->
  <section id="hero" class="h-screen flex flex-col items-center justify-center text-center px-6">
    <h2 class="text-4xl md:text-5xl font-bold mb-4">Salut, je suis <span class="text-blue-500">Peniel Mabanza</span></h2>
    <p class="text-lg md:text-xl text-gray-300 max-w-xl">
      Développeur Full Stack JavaScript 
    </p>
    <p class="text-lg md:text-xl text-gray-300 max-w-xl mt-2">
      Je conçois et développe des applications web modernes & performantes.
    </p>
  </section>

  <!-- ABOUT -->
  <section id="about" class="py-20 px-6 max-w-5xl mx-auto">
    <h3 class="text-3xl font-bold mb-6">À propos</h3>
    <p class="text-gray-300 leading-relaxed">
      Je suis un développeur Full Stack passionné par la création d'applications web performantes et intuitives. Avec une expertise en JavaScript, Node.js, et AdonisJS, je construis des solutions complètes, du front-end au back-end.
      {{-- J'aime créer des applications qui résolvent de vrais problèmes, avec une expérience utilisateur soignée. --}}
    </p>
  </section>

  <!-- SKILLS -->
  <section id="skills" class="py-20 bg-gray-800 px-6">
    <div class="max-w-5xl mx-auto">
      <h3 class="text-3xl font-bold mb-6">Compétences</h3>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-center">
        <div class="p-4 bg-gray-700 rounded-lg">HTML</div>
        <div class="p-4 bg-gray-700 rounded-lg">CSS</div>
        <div class="p-4 bg-gray-700 rounded-lg">JavaScript</div>
        <div class="p-4 bg-gray-700 rounded-lg">Node.js</div>
        <div class="p-4 bg-gray-700 rounded-lg">AdonisJS</div>
        <div class="p-4 bg-gray-700 rounded-lg">TailwindCSS</div>
        <div class="p-4 bg-gray-700 rounded-lg">PostgreSQL</div>
        <div class="p-4 bg-gray-700 rounded-lg">GitHub</div>
      </div>
    </div>
  </section>

  <!-- PROJECTS -->
  <section id="projects" class="py-20 px-6 max-w-6xl mx-auto">
    <h3 class="text-3xl font-bold mb-10">Projets</h3>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">

      <!-- PROJECT 1 -->
      <div class="bg-gray-800 p-6 rounded-xl">
        <img src="/uploads/kincliffcar.png" class="rounded-lg mb-4" alt="">
        <h4 class="text-xl font-semibold mb-2">KinCliffCar</h4>
        <p class="text-gray-400 text-sm">
          Plateforme premium de location de voitures & transport scolaire avec réservation en ligne.
        </p>
      </div>

      <!-- PROJECT 2 -->
      <div class="bg-gray-800 p-6 rounded-xl">
        <img src="/uploads/astryd.png" class="rounded-lg mb-4" alt="">
        <h4 class="text-xl font-semibold mb-2">Clinique Astryd</h4>
        <p class="text-gray-400 text-sm">
          Site web hospitalier avec système de prise de rendez-vous en ligne & présentation des services.
        </p>
      </div>

      <!-- PROJECT 3 -->
      <div class="bg-gray-800 p-6 rounded-xl">
        <img src="/uploads/lavage.png" class="rounded-lg mb-4" alt="">
        <h4 class="text-xl font-semibold mb-2">Station de Lavage</h4>
        <p class="text-gray-400 text-sm">
          Application de réservation de lavage auto moderne + lavage à domicile.
        </p>
      </div>

      <!-- PROJECT 4 -->
      <div class="bg-gray-800 p-6 rounded-xl">
        <h4 class="text-xl font-semibold mb-2">Clone de X (Twitter)</h4>
        <p class="text-gray-400 text-sm">
          Version JS + version AdonisJS : like, commentaires, retweets, inscription, connexion.
        </p>
      </div>

    </div>
  </section>

 <!-- CONTACT -->
<section id="contact" class="py-20 bg-gray-100 dark:bg-gray-800 px-6 text-center transition-colors duration-300">
  <h3 class="text-3xl font-bold mb-6">Contact</h3>
  <p class="text-gray-700 dark:text-gray-300 mb-6">
    Laisse-moi un message, un projet, une idée. Je te réponds rapidement.
  </p>

  <form id="contactForm" class="max-w-lg mx-auto bg-white dark:bg-gray-900 p-6 rounded-xl shadow-lg
    transition-colors duration-300">

    <input type="text" id="name" placeholder="Votre nom"
      class="w-full p-3 mb-4 rounded-lg bg-gray-200 dark:bg-gray-700 dark:text-white" required>

    <input type="email" id="email" placeholder="Votre email"
      class="w-full p-3 mb-4 rounded-lg bg-gray-200 dark:bg-gray-700 dark:text-white" required>

    <textarea id="message" placeholder="Votre message"
      class="w-full p-3 mb-6 h-32 rounded-lg bg-gray-200 dark:bg-gray-700 dark:text-white" required></textarea>

    <div class="flex flex-col md:flex-row gap-4 justify-center">

      <!-- WHATSAPP -->
      <button type="button" onclick="sendWhatsApp()"
        class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-semibold shadow">
        Envoyer sur WhatsApp
      </button>

      <!-- EMAIL -->
      <button type="button" onclick="sendEmail()"
        class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold shadow">
        Envoyer par Email
      </button>

    </div>
  </form>
</section>
  <script>
  function sendWhatsApp() {
    const name = document.getElementById("name").value
    const email = document.getElementById("email").value
    const message = document.getElementById("message").value

    const text = `Nouveau message depuis votre portfolio:%0A
Nom: ${name}%0A
Email: ${email}%0A
Message: ${message}`

    const phone = "2430832138096"

    window.open(`https://wa.me/${phone}?text=${text}`, "_blank")
  }

  function sendEmail() {
    const name = document.getElementById("name").value
    const email = document.getElementById("email").value
    const message = document.getElementById("message").value

    const mailto = `mailto:makuntimapeniel80@gmail.com?subject=Nouveau message de ${name}&body=
Email: ${email}%0A
Message:%0A${message}`

    window.location.href = mailto
  }
</script>

</body>
</html>
